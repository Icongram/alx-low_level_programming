#include "main.h"

/**
 * mul - multipliers two intrgers
 * @a: int to multiply to b
 * @b: int to be multiplied to a
 * Return: the result of the operation.
 */
int mul(int a, int b)
{
	c = a * b;

	return (c);
}
